export {
  genreDistribution as platformShares,
  mockTrendingCharacters,
  periodLabels,
  trendingKeywords as risingKeywords,
} from "@/lib/mock-data";

export function getCharactersByPeriod() {
  return mockTrendingCharacters;
}

import { mockTrendingCharacters } from "@/lib/mock-data";
